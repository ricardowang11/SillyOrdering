<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ShopDao">
    <insert id="addShop">
        insert into shops(shop_id,
                            shop_ownerId,
                            shop_name,
                            shop_phone,
                            shop_pic,
                            shop_status)
        values(null,
                  #{ownerId},
                  #{name},
                  #{phone},
                  #{pic},
                  #{status});
    </insert>

    <update id="createItemsTable">
        CREATE TABLE #{shopId}_items
                (item_id		    INT		    NOT NULL AUTO_INCREMENT,
                item_shopId	    INT				,
                item_name 	    CHAR(50)	NOT NULL,
                item_pic	    CHAR(255)	NOT NULL,
                item_price  	FLOAT   	NOT NULL,
                PRIMARY KEY(item_id)
                )ENGINE=INNODB;
    </update>


    <select id="getBalance" resultType="int">
        select shop_curBalance from shops where shop_id=#{shopId};
    </select>
    <update id="updateBalance">
        update shops set shop_curBalance=#{newBalance} where shop_id=#{shopId}
    </update>
    <resultMap id="shopMap" type="com.example.demo.bean.Shop">
        <id property="id" column="shop_id"/>
        <result property="ownerId" column="shop_ownerId"/>
        <result property="name" column="shop_name"/>
        <result property="phone" column="shop_phone"/>
        <result property="pic" column="shop_pic"/>
        <result property="curBalance" column="shop_curBalance"/>
        <result property="creditCard" column="shop_creditCard"/>
        <result property="status" column="shop_status"/>
        <collection property="items" ofType="com.example.demo.bean.Item" javaType="list">
            <id property="id" column="item_id"/>
            <result property="shopId" column="item_shopId"/>
            <result property="name" column="item_name"/>
            <result property="pic" column="item_pic"/>
            <result property="price" column="item_price"/>
        </collection>
    </resultMap>
    <select id="getShop" resultMap="shopMap">
--       select
--       * from shops s left join #{shopId}_items i on s.shop_id = i.item_shopId
--       where s.shop_id = #{shopId}
        select * from  shops,#{shopId}_items where shop_id=#{shopId};
    </select>

    <select id="getShops" resultMap="shopMap">
        select * from  shops,#{shopId}_items limit #{page*8},8;
    </select>

    <insert id="addItem" parameterType="com.example.demo.bean.Item">
        insert into #{shopId}_items(item_id,
                                    item_shopId,
                                    item_name,
                                    item_pic,
                                    item_price)
            values(#{id},
                  #{shopId},
                  #{name},
                  #{pic},
                  #{price});
    </insert>
    <update id="updateItem">
        update #{shopId}_items set item_name=#{newItem.name},
                                    item_pic=#{newItem.pic},
                                    item_price=#{newItem.price}
        where item_id=#{itemId};
    </update>
    <delete id="deleteItem">
        delete from #{shopId}_items where item_id=#{itemId} ;
    </delete>
</mapper>
