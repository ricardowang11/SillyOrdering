<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.OrderDao">
    <update id="updateOrder">
--         update orders set username=?,password=? where id=?;
    </update>
    <insert id="addOrder" parameterType="com.example.demo.bean.Order">
        insert into orders(         order_id,
                                    order_userId,
                                    order_shopId,
                                    order_status,
                                    order_totalPrice,
                                    order_idAndCount,
                                    order_date,
                                    order_address
                                    )
            values(#{order.id},
                  #{order.userId},
                  #{order.shopId},
                  #{order.status},
                  #{order.totalPrice},
                  #{idAndCount},
                  #{order.date},
                  #{order.address});
    </insert>
</mapper>
